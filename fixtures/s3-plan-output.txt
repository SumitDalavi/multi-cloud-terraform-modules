# Terraform S3 Module — Sample Plan Output

This is a reference plan output generated by:
```bash
cd modules/aws/s3
terraform init -backend=false
terraform plan -var="bucket_name=portfolio-test-bucket"
```

## Plan Output (Reference, 2026-08-29)

```
Terraform will perform the following actions:

  # aws_s3_bucket.secure_bucket will be created
  + resource "aws_s3_bucket" "secure_bucket" {
      + bucket                      = "portfolio-test-bucket"
      + force_destroy               = false
      + id                          = (known after apply)
      + object_lock_enabled         = false
      + tags                        = {
          + "Environment" = "production"
          + "ManagedBy"   = "terraform"
        }
    }

  # aws_s3_bucket_versioning.bucket_versioning will be created
  + resource "aws_s3_bucket_versioning" "bucket_versioning" {
      + bucket = (known after apply)
      + versioning_configuration {
          + status = "Enabled"
        }
    }

  # aws_s3_bucket_server_side_encryption_configuration.bucket_encryption will be created
  + resource "aws_s3_bucket_server_side_encryption_configuration" "bucket_encryption" {
      + bucket = (known after apply)
      + rule {
          + apply_server_side_encryption_by_default {
              + sse_algorithm = "AES256"
            }
        }
    }

  # aws_s3_bucket_public_access_block.bucket_public_access_block will be created
  + resource "aws_s3_bucket_public_access_block" "bucket_public_access_block" {
      + block_public_acls       = true
      + block_public_policy     = true
      + ignore_public_acls      = true
      + restrict_public_buckets = true
    }

Plan: 4 to add, 0 to change, 0 to destroy.
```

> **Note:** This plan is generated without cloud credentials (using `-backend=false`). Actual deployment requires valid AWS credentials.
